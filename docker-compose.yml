version: "3"
services:
  db:
    image:  postgres
    restart: always
    container_name: ${CONTAINER_NAME}
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}